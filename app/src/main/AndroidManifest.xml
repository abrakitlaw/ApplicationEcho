<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thesis.application.echo">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/myMainTheme">
        <activity
            android:name=".WelcomePage"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".view.Login" />
        <activity android:name=".view.RegisterUser" />
        <activity android:name=".view.Home" />
        <activity
            android:name=".view.MainHome"
            android:label="@string/title_activity_main_home"
            android:theme="@style/Theme.Design.Light.NoActionBar" />
        <activity android:name=".view.ResetPassword" />
        <activity android:name=".view.ResetPasswordConfirmation" />
        <activity android:name=".view.Profile" />
        <activity android:name=".view.SaveUserInformation" />
        <activity
            android:name=".view.Post"
            android:label="@string/title_activity_post"
            android:theme="@style/myMainTheme.NoActionBar"></activity>
    </application>

</manifest>